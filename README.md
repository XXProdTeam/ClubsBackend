# ClubsBackend

Бэкенд-приложение для мини-приложения "Клубы" в мессенджере MAX.

## 📋 Описание проекта

ClubsBackend - это RESTful API сервис, построенный на FastAPI, который обеспечивает функциональность для управления клубами в мессенджере MAX. Проект включает в себя:

- **FastAPI приложение** - основной веб-сервис с REST API
- **Telegram Bot** - бот для взаимодействия с пользователями
- **PostgreSQL** - база данных для хранения информации
- **Docker** - контейнеризация для простого развертывания

## 🔧 Требования

Перед началом работы убедитесь, что у вас установлено следующее ПО:

- **Docker** (версия 20.10 или выше)
- **Docker Compose** (версия 2.0 или выше)
- **Python** 3.12+ (для локальной разработки без Docker)
- **Make** (опционально, для упрощения команд)

### Проверка установленных версий:

```bash
docker --version
docker compose version
python --version
make --version
```

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/XXProdTeam/ClubsBackend.git
cd ClubsBackend
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корневой директории проекта:

```bash
cp .env.example .env  # если есть файл-пример
# или создайте новый файл
nano .env
```

Добавьте следующие переменные окружения в файл `.env`:

```env
# Настройки PostgreSQL
POSTGRES_USER=user
POSTGRES_PASSWORD=admin
POSTGRES_DB=clubsdb
POSTGRES_PORT=5432
POSTGRES_HOST=db

# Токен бота MAX
BOT_TOKEN=your_bot_token_here
```

**Важно:** Замените `your_bot_token_here` на реальный токен вашего бота.

### 3. Запуск через Docker Compose

#### Вариант A: Запуск в фоновом режиме (рекомендуется)

```bash
docker compose up -d --build
```

#### Вариант B: Запуск с выводом логов в терминал

```bash
docker compose up --build
```

#### Вариант C: Использование Makefile

```bash
# Запуск всех сервисов
make up

# Остановка всех сервисов
make down

# Просмотр доступных команд
make help
```

### 4. Проверка работы сервисов

После запуска контейнеров проверьте статус:

```bash
docker compose ps
```

Все сервисы должны иметь статус "running".

### 5. Доступ к приложению

- **API Documentation (Swagger)**: http://localhost:8000/docs
- **API Documentation (ReDoc)**: http://localhost:8000/redoc
- **Health Check**: http://localhost:8000/status
- **PostgreSQL**: localhost:5432

## 📦 Сборка и запуск контейнеров

### Сборка Docker образа

```bash
# Сборка образа приложения
docker build -t clubsbackend:latest .

# Сборка с тегом версии
docker build -t clubsbackend:v1.0.0 .
```

### Запуск отдельных контейнеров

#### Запуск базы данных

```bash
docker compose up -d db
```

#### Запуск API сервера

```bash
docker compose up -d app
```

#### Запуск бота

```bash
docker compose up -d bot
```

### Просмотр логов контейнеров

```bash
# Логи всех сервисов
docker compose logs -f

# Логи конкретного сервиса
docker compose logs -f app
docker compose logs -f bot
docker compose logs -f db

# Последние 100 строк логов
docker compose logs --tail=100 app
```

### Остановка и удаление контейнеров

```bash
# Остановка контейнеров
docker compose stop

# Остановка и удаление контейнеров
docker compose down

# Остановка с удалением volumes (ВНИМАНИЕ: удаляет данные БД)
docker compose down -v

# Остановка через Makefile
make down
```

## 💻 Локальная разработка без Docker

### 1. Установка зависимостей

#### Использование uv (рекомендуется)

```bash
# Установка uv
pip install uv

# Создание виртуального окружения
python -m uv venv .venv

# Активация виртуального окружения
# На Linux/Mac:
source .venv/bin/activate
# На Windows:
.venv\Scripts\activate

# Установка зависимостей
uv pip install -e .
```

#### Использование pip

```bash
# Создание виртуального окружения
python -m venv .venv

# Активация виртуального окружения
# На Linux/Mac:
source .venv/bin/activate
# На Windows:
.venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Запуск PostgreSQL локально

```bash
# Запуск только базы данных через Docker
docker compose up -d db
```

Или используйте локально установленный PostgreSQL и настройте переменные окружения:

```env
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
```

### 3. Запуск приложения

#### Запуск API сервера

```bash
# Базовый запуск
uvicorn app.main:app --host 0.0.0.0 --port 8000

# С автоперезагрузкой при изменениях (для разработки)
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# С указанием количества воркеров
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

#### Запуск бота

```bash
python -m bot.main
```

### 4. Линтинг и форматирование кода

```bash
# Запуск ruff для проверки кода
ruff check .

# Автоматическое исправление проблем
ruff check --fix .

# Форматирование кода
ruff format .
```

## 🏗️ Структура проекта

```
ClubsBackend/
├── app/                    # Основное приложение FastAPI
│   ├── api/               # API endpoints
│   ├── core/              # Конфигурация и настройки
│   ├── crud/              # CRUD операции с БД
│   ├── db/                # Модели базы данных
│   ├── middlewares/       # Middleware компоненты
│   ├── schemas/           # Pydantic схемы
│   ├── services/          # Бизнес-логика
│   └── main.py            # Точка входа приложения
├── bot/                   # Telegram bot
│   ├── services/          # Сервисы бота
│   ├── keyboards.py       # Клавиатуры бота
│   └── main.py            # Точка входа бота
├── nginx/                 # Конфигурация Nginx (для продакшена)
├── Dockerfile             # Dockerfile для сборки образа
├── docker-compose.yml     # Конфигурация Docker Compose
├── docker-compose.prod.yml # Продакшен конфигурация
├── Makefile               # Makefile с полезными командами
├── pyproject.toml         # Зависимости проекта (uv)
├── requirements.txt       # Зависимости проекта (pip)
└── README.md              # Этот файл
```

## 🔑 Переменные окружения

### Обязательные переменные

| Переменная | Описание | Пример значения |
|------------|----------|-----------------|
| `POSTGRES_USER` | Имя пользователя PostgreSQL | `user` |
| `POSTGRES_PASSWORD` | Пароль PostgreSQL | `admin` |
| `POSTGRES_DB` | Имя базы данных | `clubsdb` |
| `POSTGRES_PORT` | Порт PostgreSQL | `5432` |
| `POSTGRES_HOST` | Хост PostgreSQL | `db` (в Docker) / `localhost` (локально) |
| `BOT_TOKEN` | Токен бота MAX | `your_token_here` |

### Пример полного файла .env

```env
# PostgreSQL Configuration
POSTGRES_USER=user
POSTGRES_PASSWORD=strong_password_123
POSTGRES_DB=clubsdb
POSTGRES_PORT=5432
POSTGRES_HOST=db

# Bot Configuration
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

## 📝 Примеры использования API

### Проверка статуса

```bash
curl http://localhost:8000/status
```

### Работа с API через curl

```bash
# GET запрос
curl -X GET "http://localhost:8000/api/v1/endpoint" \
  -H "accept: application/json"

# POST запрос
curl -X POST "http://localhost:8000/api/v1/endpoint" \
  -H "accept: application/json" \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

### Использование Swagger UI

Откройте браузер и перейдите по адресу:
```
http://localhost:8000/docs
```

Здесь вы можете интерактивно тестировать все доступные API endpoints.

## 🔍 Полезные команды Docker

### Просмотр информации о контейнерах

```bash
# Список запущенных контейнеров
docker compose ps

# Подробная информация о контейнерах
docker compose ps -a

# Использование ресурсов
docker stats
```

### Вход в контейнер

```bash
# Вход в контейнер приложения
docker compose exec app sh

# Вход в контейнер базы данных
docker compose exec db psql -U user -d clubsdb

# Вход в контейнер бота
docker compose exec bot sh
```

### Работа с базой данных

```bash
# Подключение к PostgreSQL через docker
docker compose exec db psql -U user -d clubsdb

# Выполнение SQL команд
docker compose exec db psql -U user -d clubsdb -c "SELECT * FROM users;"

# Создание бэкапа базы данных
docker compose exec db pg_dump -U user clubsdb > backup.sql

# Восстановление из бэкапа
docker compose exec -T db psql -U user clubsdb < backup.sql
```

### Очистка и обслуживание

```bash
# Удаление неиспользуемых образов
docker image prune -a

# Удаление неиспользуемых volumes
docker volume prune

# Полная очистка Docker (ОСТОРОЖНО!)
docker system prune -a --volumes
```

## 🐛 Отладка и решение проблем

### Проблема: Контейнеры не запускаются

**Решение:**
```bash
# Проверьте логи
docker compose logs

# Проверьте, свободны ли порты
sudo lsof -i :8000
sudo lsof -i :5432

# Пересоздайте контейнеры
docker compose down
docker compose up -d --build --force-recreate
```

### Проблема: Ошибка подключения к базе данных

**Решение:**
```bash
# Проверьте статус контейнера БД
docker compose ps db

# Проверьте healthcheck БД
docker compose exec db pg_isready -U user -d clubsdb

# Проверьте переменные окружения
docker compose exec app env | grep POSTGRES
```

### Проблема: Порты уже заняты

**Решение:**
```bash
# Найдите процесс, использующий порт
sudo lsof -i :8000

# Остановите процесс
kill -9 <PID>

# Или измените порт в docker-compose.yml
```

### Проблема: База данных не создается

**Решение:**
```bash
# Удалите volume и пересоздайте
docker compose down -v
docker compose up -d

# Проверьте права доступа
docker compose exec db ls -la /var/lib/postgresql/data/
```

## 🚀 Продакшен развертывание

Для развертывания в продакшене используйте:

```bash
# С nginx и SSL сертификатами
make up

# Или напрямую через docker compose
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

**Важно:** Перед продакшен развертыванием:
1. Измените пароли в `.env` на сложные
2. Настройте SSL сертификаты
3. Настройте nginx конфигурацию
4. Включите мониторинг и логирование
5. Настройте бэкапы базы данных

## 📚 Дополнительные ресурсы

- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Docker Documentation](https://docs.docker.com/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [MaxAPI Documentation](https://github.com/vk-python/maxapi)

## 🤝 Участие в разработке

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект разработан для мини-приложения в мессенджере MAX.

## 👥 Авторы

XXProdTeam

---

**Примечание:** Если у вас возникли проблемы или вопросы, создайте Issue в репозитории GitHub.
